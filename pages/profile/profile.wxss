/* 个人中心：层级清晰 · 留白 · 低饱和（简约） */

.page-profile {
  padding-bottom: calc(32rpx + env(safe-area-inset-bottom));
  min-height: 100vh;
  background: #f1f5f9;
  position: relative;
  z-index: 1;
}

.page-profile::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: 38vh;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.95) 0%, rgba(241, 245, 249, 0) 100%);
  pointer-events: none;
  z-index: 0;
}

/* 顶栏：一级信息面 */
.page-profile .header {
  padding: 12rpx 24rpx 14rpx;
  background: #ffffff;
  border-bottom: 1rpx solid rgba(15, 23, 42, 0.06);
  position: relative;
  z-index: 10;
}

.page-profile .status-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 20rpx;
  color: var(--neo-text-3);
  margin-bottom: 8rpx;
}

.page-profile .status-text {
  line-height: 1;
}

.page-profile .top-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.page-profile .header-title-wrap {
  flex: 1;
}

.page-profile .header-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
  letter-spacing: 0.5rpx;
}

.page-profile .header-actions {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.page-profile .header-icon-wrap {
  padding: 8rpx;
  min-width: 48rpx;
  min-height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  font-size: 30rpx;
  border-radius: 14rpx;
  background: #f8fafc;
}

/* 问候：二级信息（弱于标题、强于正文） */
.page-profile .greeting-section {
  padding: 16rpx 24rpx 20rpx;
  background: #ffffff;
  border-bottom: 1rpx solid rgba(15, 23, 42, 0.06);
  display: flex;
  align-items: center;
  gap: 10rpx;
  flex-wrap: wrap;
}

.page-profile .greeting-text {
  font-size: 30rpx;
  font-weight: 600;
  color: #1e293b;
}

.page-profile .greeting-icon {
  font-size: 26rpx;
  opacity: 0.85;
}

.page-profile .greeting-date {
  margin-left: auto;
  font-size: 24rpx;
  font-weight: 400;
  color: #94a3b8;
}

/* ========== 个性名片（顶部大卡 · L1 主内容） ========== */
.profile-card.personality-card {
  margin: 20rpx 24rpx 16rpx;
  padding: 0;
  position: relative;
  overflow: hidden;
  animation: fadeInUp 0.55s cubic-bezier(0.22, 1, 0.36, 1);
  border-radius: 24rpx;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
  box-shadow: 0 4rpx 24rpx rgba(15, 23, 42, 0.06);
  background: #ffffff;
  z-index: 1;
}

.personality-card-glow {
  position: absolute;
  top: -30%;
  right: -15%;
  width: 55%;
  height: 70%;
  background: radial-gradient(circle at 70% 30%, rgba(52, 211, 153, 0.08) 0%, transparent 60%);
  pointer-events: none;
  z-index: 0;
}

.personality-card-body {
  position: relative;
  z-index: 1;
  padding: 28rpx 24rpx 24rpx;
  background: linear-gradient(180deg, #ffffff 0%, #fafbfc 100%);
}

.personality-top {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  gap: 8rpx;
  margin-bottom: 22rpx;
}

.avatar-ring {
  flex-shrink: 0;
  padding: 4rpx;
  border-radius: 50%;
  background: linear-gradient(145deg, #ecfdf5 0%, #d1fae5 55%, #e0f2fe 100%);
  border: 1rpx solid rgba(16, 185, 129, 0.18);
  box-shadow: 0 8rpx 20rpx rgba(15, 23, 42, 0.06);
}

.avatar-wrapper {
  position: relative;
}

/* 不依赖 Lottie 内 AE 表达式（eval）：用 CSS 模拟弹性缩放，与 personal.js 约 2.2s/圈 同步 */
.avatar-lottie-elastic {
  position: absolute;
  top: -6rpx;
  left: -6rpx;
  width: 132rpx;
  height: 132rpx;
  border-radius: 50%;
  overflow: hidden;
  transform-origin: 50% 50%;
  z-index: 1;
  pointer-events: none;
  animation: profileAvatarElastic 2.2s cubic-bezier(0.33, 1.15, 0.52, 1) infinite;
}

@keyframes profileAvatarElastic {
  0% {
    transform: scale(1);
  }
  9% {
    transform: scale(1.07);
  }
  18% {
    transform: scale(0.96);
  }
  27% {
    transform: scale(1.035);
  }
  36% {
    transform: scale(1);
  }
  100% {
    transform: scale(1);
  }
}

.avatar-lottie {
  display: block;
  width: 100%;
  height: 100%;
}

.avatar {
  position: relative;
  z-index: 2;
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  flex-shrink: 0;
  object-fit: cover;
  background: var(--color-bg-muted);
  border: 3rpx solid rgba(255, 255, 255, 0.8);
  box-shadow: var(--shadow-md);
  transition: transform var(--transition-normal);
}

.avatar:active {
  transform: scale(0.95);
}

.avatar-placeholder {
  position: relative;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
}

.avatar-icon {
  font-size: 68rpx;
  opacity: 0.8;
}

.avatar-badge {
  position: absolute;
  z-index: 3;
  left: 50%;
  bottom: -6rpx;
  transform: translateX(-50%);
  max-width: 112rpx;
  padding: 4rpx 12rpx;
  background: rgba(15, 23, 42, 0.78);
  border-radius: 999rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.35);
  box-shadow: 0 6rpx 16rpx rgba(15, 23, 42, 0.2);
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-badge-text {
  font-size: 18rpx;
  color: #f8fafc;
  font-weight: 600;
  white-space: nowrap;
  line-height: 1.2;
}

.personality-main {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  padding-top: 4rpx;
  margin-left: 20rpx;
}

.nickname-block {
  display: flex;
  flex-direction: row;
  align-items: center;
  flex-wrap: wrap;
  gap: 12rpx 14rpx;
  margin-bottom: 8rpx;
}

.nickname {
  font-size: 34rpx;
  font-weight: 700;
  color: #0f172a;
  letter-spacing: 0.3rpx;
  line-height: 1.3;
}

.personality-tagline {
  font-size: 22rpx;
  font-weight: 400;
  color: #94a3b8;
  line-height: 1.45;
  margin-bottom: 12rpx;
}

.user-medals {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-bottom: 8rpx;
}

.user-medal {
  width: 40rpx;
  height: 40rpx;
  border-radius: 12rpx;
  background: #f8fafc;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
  display: flex;
  align-items: center;
  justify-content: center;
}

.user-medal-icon {
  font-size: 26rpx;
  line-height: 1;
}

.signature-block {
  align-self: stretch;
  display: flex;
  flex-direction: row;
  align-items: stretch;
  gap: 14rpx;
  margin-top: 12rpx;
  padding: 18rpx 20rpx;
  border-radius: 16rpx;
  background: linear-gradient(145deg, rgba(255, 255, 255, 0.92) 0%, #f1f5f9 55%, #eef2f7 100%);
  border: 1rpx solid rgba(15, 23, 42, 0.07);
  box-shadow: 0 2rpx 12rpx rgba(15, 23, 42, 0.04);
  max-width: 100%;
  box-sizing: border-box;
}

.signature-accent-bar {
  width: 6rpx;
  min-height: 40rpx;
  align-self: stretch;
  border-radius: 6rpx;
  background: linear-gradient(180deg, #5eead4 0%, #38bdf8 45%, #22c55e 100%);
  flex-shrink: 0;
}

.signature-text {
  flex: 1;
  min-width: 0;
  font-size: 24rpx;
  color: #475569;
  line-height: 1.7;
  font-weight: 400;
  letter-spacing: 0.02em;
  word-break: break-word;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 4;
  overflow: hidden;
}

.goal-chip {
  display: inline-flex;
  align-items: center;
  gap: 6rpx;
  padding: 5rpx 12rpx;
  background: #ecfdf5;
  color: #047857;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 600;
  border: 1rpx solid rgba(16, 185, 129, 0.2);
}

.goal-chip-icon {
  font-size: 22rpx;
}

/* 社交数据：L2 次级信息区 */
.social-panel {
  display: flex;
  flex-direction: row;
  align-items: stretch;
  background: #f8fafc;
  border-radius: 16rpx;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
  overflow: hidden;
  margin-bottom: 16rpx;
}

.social-cell {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 18rpx 8rpx;
  min-width: 0;
}

.social-divider {
  width: 1rpx;
  align-self: stretch;
  margin: 16rpx 0;
  background: linear-gradient(180deg, transparent, rgba(148, 163, 184, 0.35), transparent);
}

.social-stat-num {
  font-size: 30rpx;
  font-weight: 700;
  color: #0f172a;
  line-height: 1.15;
}

.social-stat-label {
  font-size: 20rpx;
  color: #94a3b8;
  margin-top: 6rpx;
  font-weight: 500;
}

/* 坚持记录：L3 入口条（线框 + 左色条，少渐变） */
.streak-banner {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  padding: 20rpx 20rpx;
  border-radius: 16rpx;
  background: #ffffff;
  border: 1rpx solid rgba(15, 23, 42, 0.08);
  box-shadow: inset 4rpx 0 0 0 #34d399;
}

.streak-banner-left {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 14rpx;
  flex: 1;
  min-width: 0;
}

.streak-emoji {
  font-size: 36rpx;
  line-height: 1;
  flex-shrink: 0;
}

.streak-copy {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  min-width: 0;
}

.streak-title {
  font-size: 28rpx;
  font-weight: 700;
  color: #0f172a;
}

.streak-sub {
  font-size: 22rpx;
  color: #94a3b8;
  line-height: 1.45;
  font-weight: 400;
}

.streak-chevron {
  font-size: 32rpx;
  color: #cbd5e1;
  font-weight: 300;
  flex-shrink: 0;
}

/* 身体档案等卡片仍用 stat-value / stat-label 时保留 */
.stat-item {
  display: flex;
  flex-direction: column;
}

.stat-label {
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
  margin-bottom: 4rpx;
}

.stat-value {
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-bold);
  color: var(--color-success);
}

.stat-value.low {
  color: var(--color-secondary);
}

.stat-value.high {
  color: #ff9800;
}

.stat-value.very-high {
  color: #ff5252;
}

/* 卡片通用：L2 内容块（白底 · 轻阴影 · 统一边距） */
.section-card {
  margin: 0 24rpx 16rpx;
  padding: 24rpx 24rpx 26rpx;
  background: #ffffff;
  border-radius: 22rpx;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
  box-shadow: 0 2rpx 16rpx rgba(15, 23, 42, 0.04);
  animation: fadeInUp 0.5s ease-out;
  animation-fill-mode: both;
}

.section-card:nth-of-type(1) {
  animation-delay: 0.06s;
}

.section-card:nth-of-type(2) {
  animation-delay: 0.1s;
}

.section-card:nth-of-type(3) {
  animation-delay: 0.14s;
}

.section-card:nth-of-type(4) {
  animation-delay: 0.18s;
}

.section-card:nth-of-type(5) {
  animation-delay: 0.22s;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid #f1f5f9;
}

.card-title-wrapper {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.card-icon {
  font-size: 26rpx;
  opacity: 0.9;
}

.card-icon-chart {
  font-size: 28rpx;
}

.card-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #0f172a;
  letter-spacing: 0.3rpx;
}

.card-arrow {
  font-size: 28rpx;
  color: #cbd5e1;
  font-weight: 300;
}

/* 身体档案 */
.body-data {
  margin-bottom: 24rpx;
}

.data-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

/* 身体档案：数据格（浅底区分层级） */
.body-archive-card .data-item-grid {
  display: flex;
  align-items: center;
  padding: 20rpx 18rpx;
  background: #f8fafc;
  border-radius: 14rpx;
  border: 1rpx solid rgba(15, 23, 42, 0.05);
  transition: background 0.2s ease;
}

.body-archive-card .data-item-grid:active {
  transform: scale(0.98);
}

.body-archive-card .data-item-grid.highlight {
  background: #ecfdf5;
  border-color: rgba(16, 185, 129, 0.2);
  grid-column: 1 / -1;
}

.data-item-grid {
  display: flex;
  align-items: center;
  padding: 20rpx;
  background: #F7F8FA;
  border-radius: 14rpx;
  transition: all 0.3s;
}

.data-item-grid:active {
  transform: scale(0.98);
  background: #E8F5E9;
}

.data-item-grid.highlight {
  background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
  grid-column: 1 / -1;
}

.data-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
}

.data-content {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.data-label {
  font-size: 22rpx;
  color: #94a3b8;
  font-weight: 500;
  margin-bottom: 6rpx;
}

.data-value {
  font-size: 28rpx;
  font-weight: 700;
  color: #0f172a;
}

.highlight-value {
  color: #059669;
  font-size: 30rpx;
  font-weight: 700;
}

/* BMI指示器 */
.bmi-indicator {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.bmi-bar {
  width: 100%;
  height: 12rpx;
  background: #E8E8E8;
  border-radius: 6rpx;
  overflow: visible;
  position: relative;
  margin-bottom: 16rpx;
}

.bmi-marker {
  position: absolute;
  top: 100%;
  left: 0;
  width: 4rpx;
  height: 16rpx;
  margin-left: -2rpx;
  margin-top: 4rpx;
  background: #9E9E9E;
  border-radius: 2rpx;
  transition: left 0.5s ease-out;
}

.bmi-fill {
  height: 100%;
  border-radius: 6rpx;
  transition: width 0.5s ease-out;
}

.bmi-fill.low {
  background: linear-gradient(90deg, var(--color-secondary) 0%, var(--color-secondary-light) 100%);
}

.bmi-fill.normal {
  background: linear-gradient(90deg, var(--color-success) 0%, var(--color-primary-light) 100%);
}

.bmi-fill.high {
  background: linear-gradient(90deg, #FF9800 0%, #FFB74D 100%);
}

.bmi-fill.very-high {
  background: linear-gradient(90deg, #FF5252 0%, #FF8A80 100%);
}

.bmi-labels {
  display: flex;
  justify-content: space-between;
  font-size: 22rpx;
  color: var(--color-text-tertiary);
}

.bmi-label {
  flex: 1;
  text-align: center;
}

/* 三大营养素 */
.macros-section {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.macros-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #64748b;
  margin-bottom: 16rpx;
  letter-spacing: 0.5rpx;
}

.macros-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.macro-item {
  display: flex;
  flex-direction: column;
}

.macro-header {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
}

.macro-icon {
  font-size: 28rpx;
  margin-right: 12rpx;
}

.macro-name {
  flex: 1;
  font-size: 28rpx;
  color: var(--color-text-primary);
}

.macro-value {
  font-size: 28rpx;
  font-weight: bold;
  color: var(--color-success);
}

.macro-bar {
  width: 100%;
  height: 8rpx;
  background: var(--color-border-light);
  border-radius: 4rpx;
  overflow: hidden;
}

.macro-fill {
  height: 100%;
  border-radius: 4rpx;
  transition: width 0.5s ease-out;
}

.carb-fill {
  background: linear-gradient(90deg, #FF9800 0%, #FFB74D 100%);
}

.protein-fill {
  background: linear-gradient(90deg, #E53935 0%, #EF5350 100%);
}

.fat-fill {
  background: linear-gradient(90deg, #43A047 0%, #66BB6A 100%);
}

/* 每日营养素目标：图标与文字配色（橙/红/绿） */
.macro-icon-carb { filter: none; }
.macro-item.macro-carb .macro-name,
.macro-item.macro-carb .macro-value { color: #E65100; }
.macro-icon-protein { filter: none; }
.macro-item.macro-protein .macro-name,
.macro-item.macro-protein .macro-value { color: #C62828; }
.macro-icon-fat { filter: none; }
.macro-item.macro-fat .macro-name,
.macro-item.macro-fat .macro-value { color: #2E7D32; }

/* 体重图表 */
.weight-chart {
  width: 100%;
  height: 260rpx;
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--color-border-light);
  overflow: hidden;
  position: relative;
}

.chart-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #64748b;
  margin-bottom: 12rpx;
}

.chart-canvas {
  width: 100%;
  height: 100%;
  display: block;
}

/* 我的目标 */
.goal-cards {
  display: flex;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.goal-card {
  flex: 1;
  padding: 18rpx 12rpx;
  background: #f8fafc;
  border-radius: 14rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  transition: border-color 0.2s ease, background 0.2s ease;
  border: 1rpx solid rgba(15, 23, 42, 0.06);
}

.goal-card:active {
  opacity: 0.92;
}

.goal-card.active {
  background: #ecfdf5;
  border-color: rgba(16, 185, 129, 0.35);
  box-shadow: none;
}

.goal-icon {
  font-size: 42rpx;
  margin-bottom: 10rpx;
}

.goal-name {
  font-size: 26rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 6rpx;
}

.goal-desc {
  font-size: 20rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 10rpx;
}

.goal-kcal {
  font-size: 24rpx;
  color: var(--color-success);
  font-weight: bold;
}

.custom-goal {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx;
  background: #F7F8FA;
  border-radius: 12rpx;
  margin-top: 12rpx;
}

.custom-goal-text {
  font-size: 26rpx;
  color: var(--color-text-secondary);
}

.custom-goal-arrow {
  font-size: 28rpx;
  color: var(--color-text-tertiary);
}

/* 偏好设置 */
.preference-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.preference-item {
  display: flex;
  align-items: center;
  padding: 20rpx;
  background: #F7F8FA;
  border-radius: 14rpx;
  transition: all 0.3s;
}

.preference-item:active {
  transform: scale(0.98);
  background: #E8F5E9;
}

.preference-icon-wrapper {
  width: 56rpx;
  height: 56rpx;
  background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 16rpx;
  flex-shrink: 0;
}

.preference-icon {
  font-size: 28rpx;
}

.preference-content {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.preference-label {
  font-size: 26rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 10rpx;
}

.preference-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.preference-empty {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
}

.tag {
  display: inline-block;
  padding: 8rpx 16rpx;
  background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
  color: var(--color-success);
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 500;
}

.tag-warning {
  background: linear-gradient(135deg, #FFF3E0 0%, #FFE0B2 100%);
  color: #FF9800;
}

.preference-arrow {
  font-size: 32rpx;
  color: var(--color-text-tertiary);
  margin-left: 20rpx;
}

/* 账号与安全 */
.account-section-card {
  margin-bottom: calc(100rpx + env(safe-area-inset-bottom));
}

.account-actions {
  display: flex;
  flex-direction: column;
  gap: 0;
  padding: 8rpx 0 12rpx;
}

.account-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 22rpx 20rpx;
  background: #f7f8fa;
  border-radius: 14rpx;
}

.account-row:active {
  opacity: 0.92;
}

.account-row-main {
  flex: 1;
  min-width: 0;
  padding-right: 16rpx;
}

.account-row-title {
  display: block;
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 8rpx;
}

.account-row-desc {
  display: block;
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  line-height: 1.4;
}

.account-row-arrow {
  font-size: 30rpx;
  color: var(--color-text-tertiary);
  flex-shrink: 0;
}

.account-row-divider {
  height: 16rpx;
}

/* 成就统计 */
.achievements-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

.achievement-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx;
  background: linear-gradient(135deg, #F7F8FA 0%, #E8F5E9 100%);
  border-radius: 14rpx;
  transition: all 0.3s;
}

.achievement-item:active {
  transform: scale(0.95);
  box-shadow: 0 4rpx 12rpx rgba(76, 175, 80, 0.2);
}

.achievement-icon {
  font-size: 42rpx;
  margin-bottom: 10rpx;
}

.achievement-value {
  font-size: 32rpx;
  font-weight: bold;
  color: var(--color-success);
  margin-bottom: 6rpx;
}

.achievement-label {
  font-size: 22rpx;
  color: var(--color-text-secondary);
}

/* 挑战快速入口 */
.challenge-quick-entry {
  display: flex;
  align-items: center;
  padding: 30rpx;
  margin: 20rpx 0;
  background: linear-gradient(135deg, #FF6B35 0%, #FF8C5A 100%);
  border-radius: 16rpx;
  color: white;
}

.challenge-icon-wrapper {
  width: 80rpx;
  height: 80rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
}

.challenge-icon {
  font-size: 48rpx;
}

.challenge-content {
  flex: 1;
}

.challenge-title {
  font-size: 32rpx;
  font-weight: 500;
  display: block;
  margin-bottom: 8rpx;
}

.challenge-desc {
  font-size: 24rpx;
  opacity: 0.9;
  display: block;
}

.challenge-arrow {
  font-size: 32rpx;
  opacity: 0.8;
}

/* 菜单列表 */
.menu-list {
  display: flex;
  flex-direction: column;
}

.menu-item {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid var(--color-border-light);
  transition: all 0.3s ease;
}

.menu-item .menu-label-wrapper {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.menu-item:active {
  transform: translateX(4rpx);
  background: rgba(76, 175, 80, 0.02);
  border-radius: 8rpx;
  padding-left: 12rpx;
  padding-right: 12rpx;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-icon {
  width: 44rpx;
  height: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  margin-right: 16rpx;
}

.menu-label {
  flex: 1;
  font-size: 26rpx;
  color: var(--color-text-primary);
}

.core-badge-small {
  padding: 4rpx 10rpx;
  background: linear-gradient(135deg, #6BCB77 0%, #4ECDC4 100%);
  color: #FFFFFF;
  font-size: 18rpx;
  font-weight: 700;
  border-radius: 10rpx;
  margin-right: 12rpx;
  box-shadow: 0 2rpx 6rpx rgba(107, 203, 119, 0.3);
  letter-spacing: 0.5rpx;
}

.menu-desc {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  margin-top: 4rpx;
}

.menu-value {
  font-size: 26rpx;
  color: var(--color-text-tertiary);
  margin-right: 12rpx;
}

.menu-arrow {
  font-size: 32rpx;
  color: var(--color-text-tertiary);
}

/* 弹窗样式 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: fadeIn 0.3s ease-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.modal-content {
  width: 90%;
  max-width: 600rpx;
  max-height: 80vh;
  background: #fff;
  border-radius: 24rpx;
  overflow: hidden;
  animation: slideUp 0.3s ease-out;
}

@keyframes slideUp {
  from {
    transform: translateY(100rpx);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 30rpx;
  border-bottom: 1rpx solid var(--color-border-light);
}

.modal-title {
  font-size: 32rpx;
  font-weight: bold;
  color: var(--color-text-primary);
}

.modal-close {
  font-size: 36rpx;
  color: var(--color-text-tertiary);
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--color-bg-muted);
  transition: all 0.3s;
}

.modal-close:active {
  background: var(--color-border-light);
  transform: scale(0.9);
}

.modal-body {
  padding: 30rpx;
  max-height: 60vh;
  overflow-y: auto;
}

.option-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.option-item {
  padding: 16rpx 32rpx;
  border: 2rpx solid var(--color-border-light);
  border-radius: 40rpx;
  font-size: 28rpx;
  color: var(--color-text-primary);
  background: var(--color-bg-muted);
  transition: all 0.3s;
}

.option-item.selected {
  background: linear-gradient(135deg, var(--color-success) 0%, var(--color-primary-dark) 100%);
  color: #fff;
  border-color: var(--color-success);
  box-shadow: 0 2rpx 8rpx rgba(76, 175, 80, 0.3);
}

.option-item:active {
  transform: scale(0.95);
}

.spiciness-section {
  margin-top: 40rpx;
  padding-top: 40rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.spiciness-label {
  display: block;
  font-size: 28rpx;
  color: var(--color-text-primary);
  margin-bottom: 20rpx;
  text-align: center;
  font-weight: bold;
}

.spiciness-slider {
  margin: 20rpx 0;
}

.spiciness-labels {
  display: flex;
  justify-content: space-between;
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  margin-top: 12rpx;
}

.modal-footer {
  padding: 20rpx 30rpx 30rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.modal-button {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: linear-gradient(135deg, var(--color-success) 0%, var(--color-primary-dark) 100%);
  color: #fff;
  font-size: 32rpx;
  font-weight: bold;
  border-radius: 44rpx;
  border: none;
  box-shadow: 0 4rpx 12rpx rgba(76, 175, 80, 0.3);
  transition: all 0.3s;
}

.modal-button:active {
  opacity: 0.8;
  transform: scale(0.98);
}

/* 加载状态 */
.loading-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}

.loading-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid var(--color-border-light);
  border-top-color: var(--color-success);
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.loading-text {
  font-size: 28rpx;
  color: var(--color-text-secondary);
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 24rpx 20rpx;
  text-align: center;
}

.empty-icon {
  font-size: 48rpx;
  margin-bottom: 8rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 4rpx;
}

.empty-hint {
  font-size: 22rpx;
  color: #CCC;
}

.empty-state.weight-empty {
  padding: 32rpx 24rpx;
  min-height: 120rpx;
}

/* 成就徽章 */
.badges-section {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.badges-title {
  font-size: 26rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 16rpx;
}

.badges-list {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.badge-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 14rpx 20rpx;
  background: linear-gradient(135deg, #FFF9C4 0%, #FFF59D 100%);
  border-radius: 14rpx;
  min-width: 110rpx;
  transition: all 0.3s;
}

.badge-item:active {
  transform: scale(0.95);
}

.badge-icon {
  font-size: 42rpx;
  margin-bottom: 6rpx;
}

.badge-name {
  font-size: 20rpx;
  color: var(--color-text-secondary);
  text-align: center;
}

/* 我的收藏 */
.favorites-preview {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.favorite-item {
  display: flex;
  align-items: center;
  padding: 20rpx;
  background: #F7F8FA;
  border-radius: 16rpx;
  transition: all 0.3s;
}

.favorite-item:active {
  transform: scale(0.98);
  background: #E8F5E9;
}

.favorite-image {
  width: 100rpx;
  height: 100rpx;
  border-radius: 12rpx;
  margin-right: 20rpx;
  background: var(--color-border-light);
}

.favorite-image-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #FFE0B2 0%, #FFCC80 100%);
}

.favorite-icon {
  font-size: 48rpx;
  opacity: 0.8;
}

.favorite-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.favorite-name {
  font-size: 26rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 6rpx;
}

.favorite-type {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
}

.view-all-link {
  margin-top: 20rpx;
  padding: 20rpx;
  text-align: center;
  color: var(--color-success);
  font-size: 26rpx;
  border-top: 1rpx solid var(--color-border-light);
}

/* 饮食记录历史 */
.history-summary {
  display: flex;
  justify-content: space-around;
  padding: 16rpx 0;
  margin-bottom: 16rpx;
  border-bottom: 1rpx solid var(--color-border-light);
}

.summary-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.summary-label {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 8rpx;
}

.summary-value {
  font-size: 28rpx;
  font-weight: bold;
  color: var(--color-success);
}

.recent-records {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.record-item {
  display: flex;
  align-items: center;
  padding: 16rpx;
  background: #F7F8FA;
  border-radius: 12rpx;
  transition: all 0.3s;
}

.record-item:active {
  transform: scale(0.98);
  background: #E8F5E9;
}

.record-time {
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  width: 120rpx;
  margin-right: 20rpx;
}

.record-content {
  flex: 1;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.record-name {
  font-size: 26rpx;
  color: var(--color-text-primary);
}

.record-kcal {
  font-size: 24rpx;
  color: var(--color-success);
  font-weight: bold;
}

/* 健康报告 */
.report-preview {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.report-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx;
  background: linear-gradient(135deg, #F7F8FA 0%, #E8F5E9 100%);
  border-radius: 12rpx;
}

.report-label {
  font-size: 26rpx;
  color: var(--color-text-secondary);
}

.report-value {
  font-size: 28rpx;
  font-weight: bold;
  color: var(--color-success);
}

.report-actions {
  margin-top: 20rpx;
}

.report-button {
  width: 100%;
  height: 80rpx;
  line-height: 80rpx;
  background: linear-gradient(135deg, var(--color-success) 0%, var(--color-primary-dark) 100%);
  color: #fff;
  font-size: 28rpx;
  font-weight: bold;
  border-radius: 40rpx;
  border: none;
  box-shadow: 0 4rpx 12rpx rgba(76, 175, 80, 0.3);
}

.report-button:active {
  opacity: 0.8;
  transform: scale(0.98);
}

/* 周报图表 */
.weekly-chart-section {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.chart-title-small {
  font-size: 24rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 16rpx;
}

.weekly-chart {
  padding: 20rpx 0;
}

.chart-bars {
  display: flex;
  justify-content: space-around;
  align-items: flex-end;
  height: 200rpx;
  padding: 0 10rpx;
}

.chart-bar-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100%;
  margin: 0 4rpx;
}

.bar-wrapper {
  flex: 1;
  width: 100%;
  display: flex;
  align-items: flex-end;
  margin-bottom: 8rpx;
  position: relative;
}

.bar-fill {
  width: 100%;
  background: linear-gradient(180deg, var(--color-success) 0%, var(--color-primary-light) 100%);
  border-radius: 8rpx 8rpx 0 0;
  min-height: 4rpx;
  transition: height 0.5s ease-out;
  position: relative;
}

.bar-fill::after {
  content: '';
  position: absolute;
  top: -2rpx;
  left: 0;
  right: 0;
  height: 2rpx;
  background: rgba(255, 255, 255, 0.3);
}

.bar-label {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 4rpx;
}

.bar-value {
  font-size: 20rpx;
  color: var(--color-text-secondary);
  font-weight: bold;
}

/* 营养素分布 */
.nutrition-distribution {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 1rpx solid var(--color-border-light);
}

.nutrition-rings {
  display: flex;
  justify-content: space-around;
  padding: 20rpx 0;
}

.nutrition-ring {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.ring-wrapper {
  position: relative;
  width: 100rpx;
  height: 100rpx;
  margin-bottom: 10rpx;
}

.ring-progress {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  position: relative;
  overflow: hidden;
}

.ring-progress::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 50%;
  border: 12rpx solid transparent;
  border-top-color: currentColor;
  border-right-color: currentColor;
  transform: rotate(calc(var(--progress) * 3.6deg - 90deg));
  transition: transform 0.5s ease-out;
}

.carb-ring {
  color: #FF9800;
  background: conic-gradient(
    from -90deg,
    #FF9800 0deg,
    #FF9800 calc(var(--progress) * 3.6deg),
    var(--color-border-light) calc(var(--progress) * 3.6deg),
    var(--color-border-light) 360deg
  );
}

.protein-ring {
  color: var(--color-secondary);
  background: conic-gradient(
    from -90deg,
    var(--color-secondary) 0deg,
    var(--color-secondary) calc(var(--progress) * 3.6deg),
    var(--color-border-light) calc(var(--progress) * 3.6deg),
    var(--color-border-light) 360deg
  );
}

.fat-ring {
  color: var(--color-success);
  background: conic-gradient(
    from -90deg,
    var(--color-success) 0deg,
    var(--color-success) calc(var(--progress) * 3.6deg),
    var(--color-border-light) calc(var(--progress) * 3.6deg),
    var(--color-border-light) 360deg
  );
}

.ring-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  z-index: 1;
}

.ring-value {
  font-size: 24rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 4rpx;
}

.ring-label {
  font-size: 20rpx;
  color: var(--color-text-tertiary);
}

/* 提醒设置 */
.reminder-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.reminder-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx;
  background: #F7F8FA;
  border-radius: 14rpx;
  transition: all 0.3s;
}

.reminder-item:active {
  transform: scale(0.98);
}

.reminder-info {
  display: flex;
  flex-direction: column;
  flex: 1;
}

.reminder-label {
  font-size: 26rpx;
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 6rpx;
}

.reminder-desc {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
}
